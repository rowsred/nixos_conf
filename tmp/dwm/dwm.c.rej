--- dwm.c
+++ dwm.c
@@ -2208,6 +2264,7 @@ main(int argc, char *argv[])
 	if (!(dpy = XOpenDisplay(NULL)))
 		die("dwm: cannot open display");
 	checkotherwm();
 	setup();
 	/* rece condition */
+	autostart_exec(); 
 #ifdef __OpenBSD__
